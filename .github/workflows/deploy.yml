name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Update project
        run: |
          cd /home/admin/web/staycasino7.de/public_html/seo-checker/Lime-Frog
          git pull origin main

      - name: Restart service
        run: sudo systemctl restart seo-checker
        # run: sudo systemctl start seo-checker

